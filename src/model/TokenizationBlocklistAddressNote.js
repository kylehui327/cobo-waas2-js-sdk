/**
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TokenizationBlocklistAddressNote model module.
 * @module model/TokenizationBlocklistAddressNote
 */
class TokenizationBlocklistAddressNote {
    /**
     * Constructs a new <code>TokenizationBlocklistAddressNote</code>.
     * @alias module:model/TokenizationBlocklistAddressNote
     * @param address {String} The allowed address.
     */
    constructor(address) { 
        
        TokenizationBlocklistAddressNote.initialize(this, address);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, address) { 
        obj['address'] = address;
    }

    /**
     * Constructs a <code>TokenizationBlocklistAddressNote</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TokenizationBlocklistAddressNote} obj Optional instance to populate.
     * @return {module:model/TokenizationBlocklistAddressNote} The populated <code>TokenizationBlocklistAddressNote</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TokenizationBlocklistAddressNote();

            if (data.hasOwnProperty('address')) {
                obj['address'] = ApiClient.convertToType(data['address'], 'String');
            }
            if (data.hasOwnProperty('note')) {
                obj['note'] = ApiClient.convertToType(data['note'], 'String');
            }
            if (data.hasOwnProperty('created_timestamp')) {
                obj['created_timestamp'] = ApiClient.convertToType(data['created_timestamp'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TokenizationBlocklistAddressNote</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TokenizationBlocklistAddressNote</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TokenizationBlocklistAddressNote.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['address'] && !(typeof data['address'] === 'string' || data['address'] instanceof String)) {
            throw new Error("Expected the field `address` to be a primitive type in the JSON string but got " + data['address']);
        }
        // ensure the json data is a string
        if (data['note'] && !(typeof data['note'] === 'string' || data['note'] instanceof String)) {
            throw new Error("Expected the field `note` to be a primitive type in the JSON string but got " + data['note']);
        }

        return true;
    }


}

TokenizationBlocklistAddressNote.RequiredProperties = ["address"];

/**
 * The allowed address.
 * @member {String} address
 */
TokenizationBlocklistAddressNote.prototype['address'] = undefined;

/**
 * The note for the allowed address.
 * @member {String} note
 */
TokenizationBlocklistAddressNote.prototype['note'] = undefined;

/**
 * The time when the blocklist address was created, in Unix timestamp format, measured in milliseconds.
 * @member {Number} created_timestamp
 */
TokenizationBlocklistAddressNote.prototype['created_timestamp'] = undefined;






export default TokenizationBlocklistAddressNote;

