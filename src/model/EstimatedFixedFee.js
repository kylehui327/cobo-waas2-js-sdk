/**
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FeeAmount from './FeeAmount';
import FeeType from './FeeType';

/**
 * The EstimatedFixedFee model module.
 * @module model/EstimatedFixedFee
 */
class EstimatedFixedFee {
    /**
     * Constructs a new <code>EstimatedFixedFee</code>.
     * The estimated transaction fee based on the fixed fee model.
     * @alias module:model/EstimatedFixedFee
     * @implements module:model/FeeAmount
     * @param fee_amount {String} The transaction fee that you need to pay for the transaction.
     * @param fee_type {module:model/FeeType} 
     * @param token_id {String} The token ID of the transaction fee.
     */
    constructor(fee_amount, fee_type, token_id) { 
        FeeAmount.initialize(this);
        EstimatedFixedFee.initialize(this, fee_amount, fee_type, token_id);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fee_amount, fee_type, token_id) { 
        obj['fee_amount'] = fee_amount;
        obj['fee_type'] = fee_type;
        obj['token_id'] = token_id;
    }

    /**
     * Constructs a <code>EstimatedFixedFee</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EstimatedFixedFee} obj Optional instance to populate.
     * @return {module:model/EstimatedFixedFee} The populated <code>EstimatedFixedFee</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EstimatedFixedFee();
            FeeAmount.constructFromObject(data, obj);

            if (data.hasOwnProperty('fee_amount')) {
                obj['fee_amount'] = ApiClient.convertToType(data['fee_amount'], 'String');
            }
            if (data.hasOwnProperty('fee_type')) {
                obj['fee_type'] = FeeType.constructFromObject(data['fee_type']);
            }
            if (data.hasOwnProperty('token_id')) {
                obj['token_id'] = ApiClient.convertToType(data['token_id'], 'String');
            }
            if (data.hasOwnProperty('is_loop')) {
                obj['is_loop'] = ApiClient.convertToType(data['is_loop'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EstimatedFixedFee</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EstimatedFixedFee</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EstimatedFixedFee.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['fee_amount'] && !(typeof data['fee_amount'] === 'string' || data['fee_amount'] instanceof String)) {
            throw new Error("Expected the field `fee_amount` to be a primitive type in the JSON string but got " + data['fee_amount']);
        }
        // ensure the json data is a string
        if (data['token_id'] && !(typeof data['token_id'] === 'string' || data['token_id'] instanceof String)) {
            throw new Error("Expected the field `token_id` to be a primitive type in the JSON string but got " + data['token_id']);
        }

        return true;
    }


}

EstimatedFixedFee.RequiredProperties = ["fee_amount", "fee_type", "token_id"];

/**
 * The transaction fee that you need to pay for the transaction.
 * @member {String} fee_amount
 */
EstimatedFixedFee.prototype['fee_amount'] = undefined;

/**
 * @member {module:model/FeeType} fee_type
 */
EstimatedFixedFee.prototype['fee_type'] = undefined;

/**
 * The token ID of the transaction fee.
 * @member {String} token_id
 */
EstimatedFixedFee.prototype['token_id'] = undefined;

/**
 * Whether the transaction was executed as a [Cobo Loop](https://manuals.cobo.com/en/portal/custodial-wallets/cobo-loop) transfer. - `true`: The transaction was executed as a Cobo Loop transfer. - `false`: The transaction was not executed as a Cobo Loop transfer. 
 * @member {Boolean} is_loop
 */
EstimatedFixedFee.prototype['is_loop'] = undefined;


// Implement FeeAmount interface:
/**
 * The transaction fee that you need to pay for the transaction.
 * @member {String} fee_amount
 */
FeeAmount.prototype['fee_amount'] = undefined;




export default EstimatedFixedFee;

