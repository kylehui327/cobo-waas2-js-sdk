/**
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The BroadcastSignedTransactions201ResponseInner model module.
 * @module model/BroadcastSignedTransactions201ResponseInner
 */
class BroadcastSignedTransactions201ResponseInner {
    /**
     * Constructs a new <code>BroadcastSignedTransactions201ResponseInner</code>.
     * @alias module:model/BroadcastSignedTransactions201ResponseInner
     * @param request_id {String} The request ID that is used to track a transaction request.
     * @param transaction_id {String} The transaction ID of this broadcast transaction.
     */
    constructor(request_id, transaction_id) { 
        
        BroadcastSignedTransactions201ResponseInner.initialize(this, request_id, transaction_id);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, request_id, transaction_id) { 
        obj['request_id'] = request_id;
        obj['transaction_id'] = transaction_id;
    }

    /**
     * Constructs a <code>BroadcastSignedTransactions201ResponseInner</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BroadcastSignedTransactions201ResponseInner} obj Optional instance to populate.
     * @return {module:model/BroadcastSignedTransactions201ResponseInner} The populated <code>BroadcastSignedTransactions201ResponseInner</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BroadcastSignedTransactions201ResponseInner();

            if (data.hasOwnProperty('request_id')) {
                obj['request_id'] = ApiClient.convertToType(data['request_id'], 'String');
            }
            if (data.hasOwnProperty('transaction_id')) {
                obj['transaction_id'] = ApiClient.convertToType(data['transaction_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BroadcastSignedTransactions201ResponseInner</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BroadcastSignedTransactions201ResponseInner</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BroadcastSignedTransactions201ResponseInner.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['request_id'] && !(typeof data['request_id'] === 'string' || data['request_id'] instanceof String)) {
            throw new Error("Expected the field `request_id` to be a primitive type in the JSON string but got " + data['request_id']);
        }
        // ensure the json data is a string
        if (data['transaction_id'] && !(typeof data['transaction_id'] === 'string' || data['transaction_id'] instanceof String)) {
            throw new Error("Expected the field `transaction_id` to be a primitive type in the JSON string but got " + data['transaction_id']);
        }

        return true;
    }


}

BroadcastSignedTransactions201ResponseInner.RequiredProperties = ["request_id", "transaction_id"];

/**
 * The request ID that is used to track a transaction request.
 * @member {String} request_id
 */
BroadcastSignedTransactions201ResponseInner.prototype['request_id'] = undefined;

/**
 * The transaction ID of this broadcast transaction.
 * @member {String} transaction_id
 */
BroadcastSignedTransactions201ResponseInner.prototype['transaction_id'] = undefined;






export default BroadcastSignedTransactions201ResponseInner;

