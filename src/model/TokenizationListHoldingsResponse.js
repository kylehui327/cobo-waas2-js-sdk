/**
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Pagination from './Pagination';
import TokenizationHoldingInfo from './TokenizationHoldingInfo';

/**
 * The TokenizationListHoldingsResponse model module.
 * @module model/TokenizationListHoldingsResponse
 */
class TokenizationListHoldingsResponse {
    /**
     * Constructs a new <code>TokenizationListHoldingsResponse</code>.
     * @alias module:model/TokenizationListHoldingsResponse
     * @param data {Array.<module:model/TokenizationHoldingInfo>} List of token holdings.
     * @param pagination {module:model/Pagination} 
     */
    constructor(data, pagination) { 
        
        TokenizationListHoldingsResponse.initialize(this, data, pagination);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, data, pagination) { 
        obj['data'] = data;
        obj['pagination'] = pagination;
    }

    /**
     * Constructs a <code>TokenizationListHoldingsResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TokenizationListHoldingsResponse} obj Optional instance to populate.
     * @return {module:model/TokenizationListHoldingsResponse} The populated <code>TokenizationListHoldingsResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TokenizationListHoldingsResponse();

            if (data.hasOwnProperty('data')) {
                obj['data'] = ApiClient.convertToType(data['data'], [TokenizationHoldingInfo]);
            }
            if (data.hasOwnProperty('pagination')) {
                obj['pagination'] = Pagination.constructFromObject(data['pagination']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TokenizationListHoldingsResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TokenizationListHoldingsResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TokenizationListHoldingsResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['data']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['data'])) {
                throw new Error("Expected the field `data` to be an array in the JSON data but got " + data['data']);
            }
            // validate the optional field `data` (array)
            for (const item of data['data']) {
                TokenizationHoldingInfo.validateJSON(item);
            };
        }
        // validate the optional field `pagination`
        if (data['pagination']) { // data not null
          if (!!Pagination.validateJSON) {
            Pagination.validateJSON(data['pagination']);
          }
        }

        return true;
    }


}

TokenizationListHoldingsResponse.RequiredProperties = ["data", "pagination"];

/**
 * List of token holdings.
 * @member {Array.<module:model/TokenizationHoldingInfo>} data
 */
TokenizationListHoldingsResponse.prototype['data'] = undefined;

/**
 * @member {module:model/Pagination} pagination
 */
TokenizationListHoldingsResponse.prototype['pagination'] = undefined;






export default TokenizationListHoldingsResponse;

